import Layout from "@/components/layout/Layout";
import RegisterPage from "@/components/screens/Register/RegisterPage";

const registration = () => {
  return (
    <Layout title={"Registration"}>
      <RegisterPage />
    </Layout>
  );
};

export default registration;
